import React from "react";

interface QuestionFormProps {
  entity: any | null;
  quizId: string;
}

const QuestionForm = ({ entity, quizId }: QuestionFormProps) => {
  return <div>QuestionForm</div>;
};

export default QuestionForm;
